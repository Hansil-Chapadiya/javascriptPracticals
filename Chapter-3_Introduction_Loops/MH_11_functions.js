function addPlusAvg(x, y) {
    return Math.round((1 + (x + y)) / 2)
}

const sum = (p, q) => {
    return p * q;
}

let n = addPlusAvg(10, 14)
console.log(n + " Done");
console.log(sum(10, 14), " Done")